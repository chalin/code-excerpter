# https://github.com/DavidAnson/markdownlint-cli2

# Lint targets and ignores (root-only; replaces passing globs on the CLI).
globs:
  - '**/*.md'

ignores:
  - dist/**
  - node_modules/**
  - test/fixtures/updater/*/**
  - test/generated/**
  - tmp/**

config:
  default: true
  line-length: false
