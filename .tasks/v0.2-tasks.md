# `0.2` Tasks

## Current status

- [x] The repo is green.
- [x] `npm run seq -- fix test` passes.
- [x] Phase 6 real-repo validation is complete:
  - [x] `open-telemetry/opentelemetry.io`
  - [x] `dart-lang/site-www`
- [x] The old vendored updater-fixture harness is gone:
  - [x] `test/updater-goldens.test.ts` removed
  - [x] `test/fixtures/code-excerpt-updater/` removed
  - [x] repo-owned updater coverage now lives under `test/fixtures/updater/`
- [x] The inject/update API boundary reports structured issues through
      `onIssue({ kind, message })`, and the broader helper-layer rollout is in
      place.

## `0.2` release tasks

1. [x] **Revisit literal `<?code-excerpt` in prose**
   - [x] Document the current `&lt;` workaround in the spec.
   - [x] Add direct regressions for raw prose mentions and the escaped form.

2. [x] **Release prep for `0.2`**
   - [x] Review the current changelog for release sufficiency.
   - [x] Treat the remaining open items in `docs/plan.md` as post-`0.2`
         follow-ups.
   - [x] Bump from `0.2.0-dev` to `0.2.0`.

## Carried over

- [x] Remaining post-`0.2` work is moved to `.tasks/v0.3-task.md`.
