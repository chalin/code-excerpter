# `0.3` Task List

## Carry-over items

1. [ ] **Phase 5a A: site-shared updater goldens**
   - [ ] Vendor `dart-lang/site-shared/pkgs/excerpter/test_data`.
   - [ ] Add Vitest updater goldens aligned with upstream `updater_test.dart`.
   - [ ] Decide which upstream cases are permanent parity coverage versus
         candidates for migration into repo-owned fixtures.

2. [ ] **Refreshing code excerpts in non-`.md` targets**
   - [ ] Decide whether `updatePaths` / CLI should support additional target
         extensions such as `.dart` or `.jade`.
   - [ ] If supported, add coverage and update the spec/docs accordingly.

## Optional later work

1. [ ] **Possible `inject.ts` refactoring**
   - [ ] Revisit only if the current `inject.ts` shape becomes a maintenance
         problem.
   - [ ] Consider splitting scanning, PI parsing, source resolution, and
         fragment rendering into clearer modules.
